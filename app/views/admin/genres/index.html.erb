<div class="container">
<h3>ジャンル一覧・追加</h3>
</div>
  <%= render "new", genre: @genre %>

 <div class="container">
   <div class="row pt-4">
     <div class="col-4">
    <table>
      <thead>
        <tr>
          <th>ジャンル</th>
        </tr>
      </thead>
      <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <th><%= genre.name %></th>
            <th><%= link_to '編集する', edit_admin_genre_path(genre) %></th>
          </tr>
        <% end %>
      </tbody>
    </table>
    </div>
    </div>
    </div>