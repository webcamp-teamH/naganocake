<div class="container">
  <div class="row">
    <h3>商品一覧</h3><h5>（全 <%= @item.count %> 件）</h5>
  </div>
  <div class="container-fluid">
    <div class="row">
      <% @items.each do |item| %>
        <div  class="col-3 ">
          <%= link_to item_path(item.id) do %>
          <%= image_tag item.get_image(250,250) %>
          <% end %><br/>
          <%= item.name %>
          <p>￥<%= item.price %>円</p>
        </div>
      <% end %>
  </div>
</div>
<%= paginate @items %>